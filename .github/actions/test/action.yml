name: Test repository
description: Run Go tests with race detection enabled.
runs:
  using: composite
  steps:
    - name: Run Go tests with race detection
      shell: bash
      run: go test -race ./...
